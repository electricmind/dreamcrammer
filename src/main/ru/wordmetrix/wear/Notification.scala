package ru.wordmetrix.wear

import android.app.PendingIntent
import android.content.Intent
import android.support.v4.app.NotificationCompat
import android.support.v4.app.NotificationManagerCompat
import android.support.v4.app.NotificationCompat.WearableExtender

/**
 * Created by cray on 1/12/16.
 */
object Notification {

}
//
//trait Notification {
//  var notificationId: Int = 001
//  var viewIntent: Intent = new Intent(this, classOf[Nothing])
//
//
//  var viewPendingIntent: PendingIntent = PendingIntent.getActivity(this, 0, viewIntent, 0)
//  var notificationBuilder: NotificationCompat.Builder = new NotificationCompat.Builder(this).setSmallIcon(R.drawable.ic_event).setContentTitle(eventTitle).setContentText(eventLocation).setContentIntent(viewPendingIntent)
//  var notificationManager: NotificationManagerCompat = NotificationManagerCompat.from(this)
//
//
//}
